# Vite reads this file automatically during `npm run build`.
# These values are embedded INTO the compiled JS at build time.
# The Render Static Site build will use these if the dashboard env vars are missing.

VITE_API_URL=https://swarasense.onrender.com
